function Person(name, lastname, age, id) {
    this.name       = ""   || name;
    this.lastname   = ""   || lastname;
    this.age        = ""   || age;
    this.id         = ""   || id;
}

module.exports = Person;